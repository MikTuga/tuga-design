# Consumer ProGuard rules — applied to consuming apps when they use this library.
# Currently empty; tuga-design exposes only Kotlin object/sealed-class APIs
# that consumers reference directly, so default rules suffice.
